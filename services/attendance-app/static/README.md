# attendance-app static

This directory is reserved for assets that belong only to `attendance-app`.

Likely first moves:

- attendance-app icons
- admin-only mockups
- attendance-tool related assets
- split CSS if store-web and attendance-app styling diverges
