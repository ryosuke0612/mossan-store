# Services Directory

This directory is the destination for the future multi-service layout.

- `store-web/` will own the Mossan Store top, blog, and app summary pages.
- `attendance-app/` will own the attendance board application and its admin/public flows.

The current runtime is still the root-level `app.py`.
These folders are intentionally being added first as a safe intermediate step.
